
@{
    ViewData["Title"] = "Show Order";
}

@* <div class="container p-4">
    <div class="row p-3">
        <div class="col-6">
            <h2 class="text-primary">Orders</h2>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-action="AddEditOrder" asp-controller="Product">Add Order&nbsp;<i class="bi bi-plus-circle"></i></a>
        </div>
    </div>
</div> *@
<table class="table table-bordered table-striped" id="orderTable" style="width:100%">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Phone Number
            </th>
            <th>
                Order Status
            </th>
            <th>
                Order Total
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>

    @*<tbody>*@
    @*@foreach(var item in Model.products)
    {
    <tr>
    <td>
    @item.Name
    </td>
    <td>
    @item.Description
    </td>
    <td>
    @item.Price
    </td>
    <td>
    @item.CategoryId
    </td>
    <td class="group">
    <a asp-action="AddEditProduct" asp-controller="Product" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
    <a asp-action="DeleteProduct" asp-controller="Product" asp-route-id="@item.Id"><i class="bi bi-trash-fill"></i></a>
    </td>
    </tr>
    }*@
    @*</tbody>*@
</table>

@section scripts {
    <script src="~/js/Order.js"></script>
}